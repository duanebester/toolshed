# Build artifacts
*.exe
*.test
*.out

# SSH host keys — private keys must never be committed.
# The server auto-generates these on first run.
.ssh/

# Go
vendor/

# OS junk
.DS_Store
Thumbs.db

# Editor / IDE
.vscode/
.idea/
*.swp
*.swo
*~

# ONNX Runtime shared libraries (downloaded by setup-embeddings.sh)
lib/

# Model weights (downloaded by setup-embeddings.sh / download-model.sh)
models/

# Environment files — may contain secrets
.env
.env.*
